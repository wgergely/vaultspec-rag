[build-system]`nbuild-backend = "hatchling.build"`nrequires = ["hatchling"]`n`n[project]`nname = "vaultspec-rag"`nversion = "0.1.0"`ndescription = "RAG module for vaultspec (ML-heavy extraction)"`nreadme = "README.md"`nrequires-python = ">=3.13"`nlicense = { text = "MIT" }`nauthors = [{ name = "Gergely Wootsch", email = "hello@gergely-wootsch.com" }]`ndependencies = [`n    "torch>=2.9.0",`n    "sentence-transformers>=5.0.0",`n    "lancedb>=0.27.0",`n    "einops>=0.8.0",`n    "pydantic>=2.12.5",`n    "rich>=14.3.2",`n]`n`n[project.optional-dependencies]`ndev = [`n    "pytest>=9.0.2",`n    "pytest-asyncio>=1.3.0",`n    "pytest-timeout>=2.4.0",`n    "ruff>=0.15.2",`n]`n`n[tool.hatch.build.targets.wheel]`npackages = ["src/vaultspec_rag"]`n`n[tool.pytest.ini_options]`nasyncio_mode = "auto"`ntestpaths = ["tests", "src"]
